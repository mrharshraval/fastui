# FastUI Worker Configuration (Optional - sensible defaults are built in)
ENVIRONMENT=production
LOG_LEVEL=INFO
PORT=8001
HEADLESS_BROWSER=true
MAX_CONCURRENT_SCRAPERS=1
# Authentication (Application-level secret shared between API and Worker)
WORKER_TOKEN=
